--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -12160,6 +12160,9 @@ Check your passwords anytime in <ph name
         <message name="IDS_TAB_CXMENU_CLOSETABSBELOW" desc="The label of the 'Close Tabs Below' Tab context menu item.">
           Close tabs below
         </message>
+        <message name="IDS_TAB_CXMENU_CLOSETABSABOVE" desc="The label of the 'Close Tabs Above' Tab context menu item.">
+          Close tabs above
+        </message>
         <message name="IDS_TAB_CXMENU_CLOSEALLTABS" desc="The label of the 'Close All Tabs' Tab context menu item.">
           Close all tabs
         </message>
@@ -12282,6 +12285,9 @@ Check your passwords anytime in <ph name
         <message name="IDS_TAB_CXMENU_CLOSETABSBELOW" desc="In Title Case: The label of the 'Close Tabs Below' Tab context menu item.">
           Close Tabs Below
         </message>
+        <message name="IDS_TAB_CXMENU_CLOSETABSABOVE" desc="In Title Case: The label of the 'Close Tabs Above' Tab context menu item.">
+          Close Tabs Above
+        </message>
         <message name="IDS_TAB_CXMENU_CLOSEALLTABS" desc="In Title Case: The label of the 'Close All Tabs' Tab context menu item.">
           Close All Tabs
         </message>
--- a/chrome/browser/ui/tabs/tab_menu_model.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model.cc
@@ -632,6 +632,19 @@ void TabMenuModel::Build(int index) {
                       IDS_TAB_CXMENU_CLOSEOTHERTABS);
 
   if (showing_vertical_tabs) {
+    AddItemWithStringId(TabStripModel::CommandCloseTabsToLeft,
+                        IDS_TAB_CXMENU_CLOSETABSABOVE);
+  } else {
+    AddItemWithStringId(TabStripModel::CommandCloseTabsToLeft,
+                        base::i18n::IsRTL() ? IDS_TAB_CXMENU_CLOSETABSTORIGHT
+                                            : IDS_TAB_CXMENU_CLOSETABSTOLEFT);
+  }
+
+  SetEnabledAt(GetItemCount() - 1,
+               tab_strip_->IsContextMenuCommandEnabled(
+                   index, TabStripModel::CommandCloseTabsToLeft));
+
+  if (showing_vertical_tabs) {
     AddItemWithStringId(TabStripModel::CommandCloseTabsToRight,
                         IDS_TAB_CXMENU_CLOSETABSBELOW);
   } else {
--- a/chrome/browser/ui/tabs/tab_strip_model.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model.cc
@@ -2690,6 +2690,7 @@ bool TabStripModel::IsContextMenuCommand
     }
 
     case CommandCloseOtherTabs:
+    case CommandCloseTabsToLeft:
     case CommandCloseTabsToRight: {
       return !GetIndicesClosedByCommand(context_index, command_id).empty();
     }
@@ -2896,6 +2897,7 @@ void TabStripModel::ExecuteContextMenuCo
       break;
     }
 
+    case CommandCloseTabsToLeft:
     case CommandCloseTabsToRight: {
       base::UmaHistogramCounts1000(
           "Tab.ContextMenu.CloseTabsToRight.SelectedTabsCount",
@@ -3756,15 +3758,22 @@ std::vector<int> TabStripModel::GetIndic
   if (!ContainsIndex(index)) {
     return indices;
   }
-  DCHECK(id == CommandCloseTabsToRight || id == CommandCloseOtherTabs);
+  DCHECK(id == CommandCloseTabsToRight || id == CommandCloseTabsToLeft ||
+         id == CommandCloseOtherTabs);
   bool is_selected = IsTabSelected(index);
   int last_unclosed_tab = -1;
+  int first_unclosed_tab = count();
   if (id == CommandCloseTabsToRight) {
     last_unclosed_tab = is_selected ? *selection_model()
                                            .GetListSelectionModel()
                                            .selected_indices()
                                            .rbegin()
                                     : index;
+  } else if (id == CommandCloseTabsToLeft) {
+    first_unclosed_tab =
+        is_selected
+            ? *selection_model_.GetListSelectionModel().selected_indices().begin()
+            : index;
   }
 
   // If the tab that the context menu command is invoked on is not selected and
@@ -3780,6 +3789,10 @@ std::vector<int> TabStripModel::GetIndic
   // NOTE: callers expect the vector to be sorted in descending order.
   std::optional<tab_groups::TabGroupId> focused_group = GetFocusedGroup();
   for (int i = count() - 1; i > last_unclosed_tab; --i) {
+    if (id == CommandCloseTabsToLeft && i >= first_unclosed_tab) {
+      continue;
+    }
+
     // Skip tabs that are not part of the focused group.
     if (focused_group.has_value() && GetTabGroupForTab(i) != focused_group) {
       continue;
@@ -3799,7 +3812,8 @@ std::vector<tabs::TabInterface*> TabStri
     return {};
   }
 
-  DCHECK(id == CommandCloseTabsToRight || id == CommandCloseOtherTabs);
+  DCHECK(id == CommandCloseTabsToRight || id == CommandCloseTabsToLeft ||
+         id == CommandCloseOtherTabs);
 
   tabs::TabInterface* invoked_tab = GetTabAtIndex(index);
 
@@ -3822,6 +3836,10 @@ std::vector<tabs::TabInterface*> TabStri
     }
 
     if (tab == invoked_tab || tab->IsPinned()) {
+      if (id == CommandCloseTabsToLeft && tab == invoked_tab) {
+        break;
+      }
+
       continue;
     }
 
--- a/components/tabs/public/tab_context_menu_command.h
+++ b/components/tabs/public/tab_context_menu_command.h
@@ -16,6 +16,7 @@ enum TabContextMenuCommand {
   CommandCloseTab,
   CommandCloseOtherTabs,
   CommandCloseTabsToRight,
+  CommandCloseTabsToLeft,
   CommandTogglePinned,
   CommandToggleGrouped,
   CommandToggleSiteMuted,
