--- a/third_party/blink/renderer/core/input/event_handler.cc
+++ b/third_party/blink/renderer/core/input/event_handler.cc
@@ -90,6 +90,7 @@
 #include "third_party/blink/renderer/core/loader/resource/image_resource_content.h"
 #include "third_party/blink/renderer/core/page/autoscroll_controller.h"
 #include "third_party/blink/renderer/core/page/chrome_client.h"
+#include "third_party/blink/renderer/core/page/context_menu_controller.h"
 #include "third_party/blink/renderer/core/page/drag_state.h"
 #include "third_party/blink/renderer/core/page/frame_tree.h"
 #include "third_party/blink/renderer/core/page/page.h"
@@ -2206,6 +2207,16 @@ WebInputEventResult EventHandler::SendCo
   // |SelectionController::sendContextMenuEvent()|.
   document.UpdateStyleAndLayout(DocumentUpdateReason::kContextMenu);
 
+  if (event.button == WebPointerProperties::Button::kRight &&
+      (event.GetModifiers() & WebInputEvent::Modifiers::kShiftKey)) {
+    Page* page = document.GetPage();
+    if (page && page->GetContextMenuController().ShowContextMenuForUserInput(
+                    frame_, position_in_contents, event.GetMenuSourceType())) {
+      return WebInputEventResult::kHandledSystem;
+    }
+    return WebInputEventResult::kNotHandled;
+  }
+
   Element* target_element = override_target_element
                                 ? override_target_element
                                 : mev.InnerPossiblyPseudoElement();
--- a/third_party/blink/renderer/core/page/context_menu_controller.cc
+++ b/third_party/blink/renderer/core/page/context_menu_controller.cc
@@ -460,6 +460,13 @@ bool ContextMenuController::ShowContextM
   return ShowContextMenu(frame, point, source_type, nullptr);
 }
 
+bool ContextMenuController::ShowContextMenuForUserInput(
+    LocalFrame* frame,
+    const PhysicalOffset& point,
+    ui::mojom::blink::MenuSourceType source_type) {
+  return ShowContextMenu(frame, point, source_type);
+}
+
 bool ContextMenuController::ShowContextMenu(
     LocalFrame* frame,
     const PhysicalOffset& point,
--- a/third_party/blink/renderer/core/page/context_menu_controller.h
+++ b/third_party/blink/renderer/core/page/context_menu_controller.h
@@ -63,6 +63,10 @@ class CORE_EXPORT ContextMenuController
                               float y,
                               ContextMenuProvider*);
 
+  bool ShowContextMenuForUserInput(LocalFrame*,
+                                   const PhysicalOffset&,
+                                   ui::mojom::blink::MenuSourceType);
+
   void CustomContextMenuItemSelected(unsigned action);
 
   Node* ContextMenuImageNodeForFrame(LocalFrame*);
