--- a/chrome/app/chrome_command_ids.h
+++ b/chrome/app/chrome_command_ids.h
@@ -183,6 +183,7 @@
 #define IDC_CUT                         36000
 #define IDC_COPY                        36001
 #define IDC_PASTE                       36003
+#define IDC_FORCE_PASTE                 36005
 
 // Find-in-page
 #define IDC_FIND                        37000
@@ -360,6 +361,7 @@
 #define IDC_CONTENT_CONTEXT_ROTATECW 50142
 #define IDC_CONTENT_CONTEXT_ROTATECCW 50143
 // Edit items.
+#define IDC_CONTENT_CONTEXT_FORCE_PASTE 50149
 #define IDC_CONTENT_CONTEXT_COPY 50150
 #define IDC_CONTENT_CONTEXT_CUT 50151
 #define IDC_CONTENT_CONTEXT_PASTE 50152
--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -1570,6 +1570,9 @@ are declared in tools/grit/grit_args.gni
           <message name="IDS_PASTE" desc="The text label of the Paste menu item" meaning="'Paste' menu command in sentence case">
             &amp;Paste
           </message>
+          <message name="IDS_FORCE_PASTE" desc="The label for a command that pastes text without invoking page paste handlers">
+            Force paste
+          </message>
           <message name="IDS_DELETE" desc="The text label of the Delete menu item (which deletes selected text without copying to clipboard)">
             &amp;Delete
           </message>
@@ -1676,6 +1679,9 @@ are declared in tools/grit/grit_args.gni
           <message name="IDS_PASTE" desc="In Title Case: The text label of the Paste menu item" meaning="'Paste' menu command for Mac">
             &amp;Paste
           </message>
+          <message name="IDS_FORCE_PASTE" desc="In Title Case: The label for a command that pastes text without invoking page paste handlers">
+            Force Paste
+          </message>
           <message name="IDS_DELETE" desc="In Title Case: The text label of the Delete menu item (which deletes selected text without copying to clipboard)">
             &amp;Delete
           </message>
--- a/chrome/browser/renderer_context_menu/render_view_context_menu.cc
+++ b/chrome/browser/renderer_context_menu/render_view_context_menu.cc
@@ -657,13 +657,14 @@ int UmaEnumForCommand(int key, UmaEnumId
        {IDC_SPELLCHECK_REMOVE_FROM_DICTIONARY, 167},
        {IDC_CONTENT_CONTEXT_SAVE_TO_MEMORY_BANKS, 168},
        {IDC_CONTENT_CONTEXT_OPENLINK_ISOLATED, 169},
+       {IDC_CONTENT_CONTEXT_FORCE_PASTE, 170},
        // To add new items:
        //   - Add one more line above this comment block, using the UMA value
        //     from the line below this comment block.
        //   - Increment the UMA value in that latter line.
        //   - Add the new item to the RenderViewContextMenuItem enum in
        //     tools/metrics/histograms/metadata/ui/enums.xml.
-       {kUmaMaxValueKey, 170}});
+       {kUmaMaxValueKey, 171}});
   // LINT.ThenChange(//tools/metrics/histograms/metadata/ui/enums.xml:RenderViewContextMenuItem)
 
   // LINT.IfChange(ContextMenuOptionDesktop)
@@ -3039,6 +3040,8 @@ void RenderViewContextMenu::AppendOtherE
     menu_model_.AddItemWithStringId(IDC_CONTENT_CONTEXT_PASTE_AND_MATCH_STYLE,
                                     IDS_CONTENT_CONTEXT_PASTE_AND_MATCH_STYLE);
   }
+  menu_model_.AddItemWithStringId(IDC_CONTENT_CONTEXT_FORCE_PASTE,
+                                  IDS_FORCE_PASTE);
 
 #if BUILDFLAG(IS_CHROMEOS)
   // Insert a submenu of clipboard history descriptors.
@@ -3496,6 +3499,7 @@ bool RenderViewContextMenu::IsCommandIdE
       return IsPasteEnabled();
 
     case IDC_CONTENT_CONTEXT_PASTE_AND_MATCH_STYLE:
+    case IDC_CONTENT_CONTEXT_FORCE_PASTE:
       return IsPasteAndMatchStyleEnabled();
 
     case IDC_CONTENT_CONTEXT_DELETE:
@@ -4084,6 +4088,10 @@ void RenderViewContextMenu::ExecuteComma
       source_web_contents_->PasteAndMatchStyle();
       break;
 
+    case IDC_CONTENT_CONTEXT_FORCE_PASTE:
+      chrome::ForcePaste(source_web_contents_);
+      break;
+
     case IDC_CONTENT_CONTEXT_DELETE:
       source_web_contents_->Delete();
       break;
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -1052,6 +1052,9 @@ void BrowserCommandController::HandleCom
     case IDC_SHARING_HUB_SCREENSHOT:
       ScreenshotCapture(browser_);
       break;
+    case IDC_FORCE_PASTE:
+      ForcePaste(browser_);
+      break;
 
     // Clipboard commands
     case IDC_CUT:
@@ -2061,6 +2064,7 @@ void BrowserCommandController::InitComma
   UpdateGlicState();
 
   // Initialize other commands whose state changes based on various conditions.
+  command_updater_->UpdateCommandEnabled(IDC_FORCE_PASTE, true);
   UpdateCommandsForFullscreenMode();
   UpdateCommandsForContentRestrictionState();
   UpdateCommandsForBookmarkEditing();
--- a/chrome/browser/ui/browser_commands.cc
+++ b/chrome/browser/ui/browser_commands.cc
@@ -190,10 +190,12 @@
 #include "components/zoom/zoom_controller.h"
 #include "content/public/browser/browsing_data_remover.h"
 #include "content/public/browser/child_process_security_policy.h"
+#include "content/public/browser/clipboard_types.h"
 #include "content/public/browser/devtools_agent_host.h"
 #include "content/public/browser/navigation_controller.h"
 #include "content/public/browser/navigation_entry.h"
 #include "content/public/browser/page_navigator.h"
+#include "content/public/browser/render_frame_host.h"
 #include "content/public/browser/render_view_host.h"
 #include "content/public/browser/render_widget_host_view.h"
 #include "content/public/browser/web_contents.h"
@@ -658,6 +660,19 @@ using content::WebContents;
 namespace chrome {
 namespace {
 
+void OnForcePasteTextRead(base::WeakPtr<content::WebContents> web_contents,
+                          content::GlobalRenderFrameHostId focused_frame_id,
+                          std::u16string text) {
+  if (!web_contents || text.empty()) {
+    return;
+  }
+
+  content::RenderFrameHost* focused_frame = web_contents->GetFocusedFrame();
+  if (focused_frame && focused_frame->GetGlobalId() == focused_frame_id) {
+    web_contents->InsertText(text);
+  }
+}
+
 #if BUILDFLAG(ENABLE_EXTENSIONS)
 const extensions::Extension* GetExtensionForBrowser(
     BrowserWindowInterface* browser) {
@@ -2921,6 +2936,30 @@ bool CanCopyUrl(BrowserWindowInterface*
          !sharing_hub::SharingIsDisabledByPolicy(browser->GetProfile());
 }
 
+void ForcePaste(BrowserWindowInterface* browser) {
+  if (!browser) {
+    return;
+  }
+  ForcePaste(browser->GetTabStripModel()->GetActiveWebContents());
+}
+
+void ForcePaste(content::WebContents* web_contents) {
+  if (!web_contents) {
+    return;
+  }
+
+  content::RenderFrameHost* focused_frame = web_contents->GetFocusedFrame();
+  if (!focused_frame) {
+    return;
+  }
+
+  ui::Clipboard::GetForCurrentThread()->ReadText(
+      ui::ClipboardBuffer::kCopyPaste,
+      content::CreateDataEndpoint(*focused_frame),
+      base::BindOnce(&OnForcePasteTextRead, web_contents->GetWeakPtr(),
+                     focused_frame->GetGlobalId()));
+}
+
 bool IsWebAppOrCustomTab(const BrowserWindowInterface* browser) {
   return web_app::AppBrowserController::IsWebApp(browser);
 }
--- a/chrome/browser/ui/browser_commands.h
+++ b/chrome/browser/ui/browser_commands.h
@@ -357,6 +357,8 @@ void CopyURL(BrowserWindowInterface* bro
              content::WebContents* web_contents);
 void CopyURLs(const std::vector<content::WebContents*>& web_contentses);
 bool CanCopyUrl(BrowserWindowInterface* browser);
+void ForcePaste(BrowserWindowInterface* browser);
+void ForcePaste(content::WebContents* web_contents);
 // Returns true if the browser window is for a web app or custom tab.
 bool IsWebAppOrCustomTab(const BrowserWindowInterface* browser);
 // Moves the WebContents of a hosted app Browser to a tabbed Browser. Returns
--- a/tools/metrics/histograms/metadata/ui/enums.xml
+++ b/tools/metrics/histograms/metadata/ui/enums.xml
@@ -414,6 +414,7 @@ chromium-metrics-reviews@google.com.
   <int value="167" label="IDC_SPELLCHECK_REMOVE_FROM_DICTIONARY"/>
   <int value="168" label="IDC_CONTENT_CONTEXT_SAVE_TO_MEMORY_BANKS"/>
   <int value="169" label="IDC_CONTENT_CONTEXT_OPENLINK_ISOLATED"/>
+  <int value="170" label="IDC_CONTENT_CONTEXT_FORCE_PASTE"/>
 </enum>
 
 <!-- LINT.ThenChange(//chrome/browser/renderer_context_menu/render_view_context_menu.cc:RenderViewContextMenuItem) -->
--- a/content/browser/web_contents/web_contents_impl.cc
+++ b/content/browser/web_contents/web_contents_impl.cc
@@ -7073,6 +7073,12 @@ void WebContentsImpl::Replace(const std:
   input_handler->Replace(word);
 }
 
+void WebContentsImpl::InsertText(const std::u16string& text) {
+  OPTIONAL_TRACE_EVENT0("content", "WebContentsImpl::InsertText");
+  last_interaction_time_ = ui::EventTimeForNow();
+  ExecuteEditCommand("InsertText", text);
+}
+
 void WebContentsImpl::ReplaceMisspelling(const std::u16string& word) {
   OPTIONAL_TRACE_EVENT0("content", "WebContentsImpl::ReplaceMisspelling");
   auto* input_handler = GetFocusedFrameWidgetInputHandler();
--- a/content/browser/web_contents/web_contents_impl.h
+++ b/content/browser/web_contents/web_contents_impl.h
@@ -551,6 +551,7 @@ class CONTENT_EXPORT WebContentsImpl
   void ScrollToTopOfDocument() override;
   void ScrollToBottomOfDocument() override;
   void Replace(const std::u16string& word) override;
+  void InsertText(const std::u16string& text) override;
   void ReplaceMisspelling(const std::u16string& word) override;
   void NotifyContextMenuClosed(const GURL& link_followed) override;
   void ExecuteCustomContextMenuCommand(int action,
--- a/content/public/browser/web_contents.h
+++ b/content/public/browser/web_contents.h
@@ -1225,6 +1225,9 @@ class WebContents : public PageNavigator
   // Replaces the currently selected word or a word around the cursor.
   virtual void Replace(const std::u16string& word) = 0;
 
+  // Inserts text at the cursor or replaces the current selection.
+  virtual void InsertText(const std::u16string& text) = 0;
+
   // Replaces the misspelling in the current selection.
   virtual void ReplaceMisspelling(const std::u16string& word) = 0;
 
